{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Сборка C++",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "-O0",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "group": "build",
            "problemMatcher": "$gcc",
            "detail": "Сборка текущего C файла"
        },
        {
            "label": "Сборка C++ без отладки",
            "type": "shell",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "group": "build",
            "problemMatcher": "$gcc",
            "detail": "Сборка текущего C файла"
        }
    ]
}