#include <catch2/catch_all.hpp>

TEST_CASE("Matrix2::Matrix2(Args&&...)", "[Matrix2], [constructors]") {
    SECTION("Matrix2::Matrix2(Args&&...) : simple check") {

    }
}

TEST_CASE("Matrix3::Matrix3(Args&&...)", "[Matrix3], [constructors]") {
    
}